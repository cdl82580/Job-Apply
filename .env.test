# UI Test credentials — copy this file to .env.test.local and fill in passwords
# Never commit .env.test.local to git

UI_BASE_URL=https://job-apply-corey.fly.dev

# Standard test user (role: user)
UI_TEST_EMAIL=cdl825+testuser@gmail.com
UI_TEST_PASSWORD=

# Admin test user (role: admin)
UI_ADMIN_EMAIL=cdl825+testadmin@gmail.com
UI_ADMIN_PASSWORD=

# To run UI tests:
#   source .env.test.local && make test-ui
#   source .env.test.local && make test-ui-admin
#   source .env.test.local && make test-ui-anon
