[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --cov=api
    --cov=apply
    --cov=scripts
    --cov=routers
    --cov-report=term-missing
    --cov-report=html:htmlcov
    -p no:warnings
