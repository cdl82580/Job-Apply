{
  "info": {
    "name": "Job Apply API",
    "description": "Complete API reference for the Job Application Agent (https://job-apply-corey.fly.dev/).\n\n**Auth model:** stateless HMAC-signed `HttpOnly` session cookie (`session`, 30-day TTL). Login/register set the cookie automatically. All protected requests include it. Admins also accepted via `Authorization: Bearer {BOT_API_KEY}` header.\n\n**Run lifecycle (application & prep):**\n1. POST to start \u2192 returns `run_id` / `prep_id`\n2. GET `\u2026/stream` \u2192 SSE; events: `progress` (log line), `done` (files + folder_url + optional replacements_warning), `error`\n3. `done` event contains `files` with filenames\n4. GET `\u2026/files/{name}` \u2192 download DOCX\n\n**Rate limits (per IP, sliding window):**\n- Login: 10/min\n- Register: 5/hr\n- Resend-verification: 3/hr\n- Change password: 5/hr\n- Change email: 5/hr\n- Resume upload: 10/hr\n\n**Security headers on every response:** HSTS (2-year), X-Frame-Options: DENY, X-Content-Type-Options: nosniff, CSP, Referrer-Policy.\n\n**Base URL:** `{{base_url}}` defaults to `https://job-apply-corey.fly.dev`. Override with `http://localhost:8000` for local dev.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "base_url",
      "value": "https://job-apply-corey.fly.dev",
      "type": "string"
    },
    {
      "key": "run_id",
      "value": "",
      "type": "string",
      "description": "UUID from POST /api/run"
    },
    {
      "key": "prep_id",
      "value": "",
      "type": "string",
      "description": "UUID from POST /api/prep"
    },
    {
      "key": "app_id",
      "value": "",
      "type": "string",
      "description": "UUID from POST /api/applications"
    },
    {
      "key": "event_id",
      "value": "",
      "type": "string",
      "description": "UUID from POST /api/calendar"
    },
    {
      "key": "comment_id",
      "value": "",
      "type": "string",
      "description": "UUID from POST /api/applications/{id}/comments"
    },
    {
      "key": "webhook_id",
      "value": "",
      "type": "string",
      "description": "UUID from POST /api/admin/webhooks"
    },
    {
      "key": "folder_id",
      "value": "",
      "type": "string",
      "description": "Google Drive folder ID from GET /api/gdrive/runs"
    },
    {
      "key": "user_id",
      "value": "",
      "type": "string",
      "description": "User UUID for admin endpoints"
    }
  ],
  "item": [
    {
      "name": "Health",
      "item": [
        {
          "name": "GET /api/health",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/health",
            "description": "Liveness check. Unauthenticated callers get `{\"status\": \"ok\"|\"degraded\"}` only. Authenticated users also receive storage, email, gdrive, model, anthropic, and fly machine details."
          },
          "response": [
            {
              "name": "200 OK (unauthenticated)",
              "status": "OK",
              "code": 200,
              "body": "{\"status\": \"ok\"}"
            },
            {
              "name": "200 OK (authenticated)",
              "status": "OK",
              "code": 200,
              "body": "{\"status\": \"ok\", \"storage\": \"ok\", \"email\": \"configured\", \"gdrive\": \"configured\", \"model\": \"claude-sonnet-4-6\", \"anthropic\": \"configured\", \"fly_machine\": \"abc123\", \"fly_app\": \"job-apply-corey\"}"
            }
          ]
        }
      ]
    },
    {
      "name": "Auth",
      "description": "Session-based auth using an HMAC-signed `HttpOnly` cookie (30-day TTL). Google OAuth also supported. Changing email or password invalidates existing sessions via a password-version fingerprint (`pwv`) embedded in the token.",
      "item": [
        {
          "name": "POST /api/auth/register",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/auth/register",
            "description": "Create account. `multipart/form-data` so resume DOCX can be included. Sets session cookie on success. Sends verification email via Resend. Rate-limited: 5/hr per IP.",
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "display_name",
                  "value": "Corey Laverdiere",
                  "type": "text"
                },
                {
                  "key": "email",
                  "value": "you@example.com",
                  "type": "text"
                },
                {
                  "key": "password",
                  "value": "minimum8chars",
                  "type": "text"
                },
                {
                  "key": "profile_text",
                  "value": "## Voice\u2026",
                  "type": "text"
                },
                {
                  "key": "resume",
                  "value": "",
                  "type": "file"
                }
              ]
            }
          }
        },
        {
          "name": "POST /api/auth/login",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/auth/login",
            "description": "Authenticate with email and password. Sets session cookie. Constant-time comparison prevents user-enumeration timing attacks. Rate-limited: 10/min per IP.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"you@example.com\",\n  \"password\": \"your-password\"\n}"
            }
          }
        },
        {
          "name": "POST /api/auth/logout",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/auth/logout",
            "description": "Clears session cookie and fly-force-instance-id cookie."
          }
        },
        {
          "name": "GET /api/auth/me",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/auth/me",
            "description": "Current user info: `user_id`, `email`, `display_name`, `role`, `email_verified`, `has_resume`, `has_profile`, `model` (active Claude model)."
          }
        },
        {
          "name": "GET /api/auth/google",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/auth/google?returnTo=/tracking.html",
            "description": "Redirects to Google consent screen. `returnTo` is the path to redirect after auth (clamped to relative paths; admins always go to /admin.html). A nonce is set as an HttpOnly cookie for CSRF protection."
          }
        },
        {
          "name": "GET /api/auth/google/callback",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/auth/google/callback",
            "description": "Google OAuth callback. Handled by the browser redirect \u2014 do not call directly. Verifies CSRF nonce, exchanges code for tokens, verifies id_token via google-auth, finds or creates user, sets session cookie."
          }
        },
        {
          "name": "GET /api/auth/verify-email",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/auth/verify-email?token=TOKEN",
            "description": "Public endpoint. Consumes a one-time SHA-256-keyed verification token (72h TTL), marks user verified, redirects to /login.html?verified=1. Invalid/expired tokens redirect with an error message."
          }
        },
        {
          "name": "POST /api/auth/resend-verification",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/auth/resend-verification",
            "description": "Resend verification email for the current user. Returns `{\"ok\": true, \"already_verified\": true}` if already verified. Rate-limited: 3/hr per IP."
          }
        }
      ]
    },
    {
      "name": "Profile",
      "description": "Manage display name, profile guide (Markdown), master resume DOCX, password, and email. Password and email changes are rate-limited and invalidate existing sessions.",
      "item": [
        {
          "name": "GET /api/profile",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/profile",
            "description": "Returns: `display_name`, `email`, `role`, `email_verified`, `profile_text`, `has_resume`, `resume_filename`."
          }
        },
        {
          "name": "PUT /api/profile",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/profile",
            "description": "Update display name and/or profile guide text. Both optional. Max display_name: 100 chars; max profile_text: 50,000 chars.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"display_name\": \"Corey L.\",\n  \"profile_text\": \"Updated guide\u2026\"\n}"
            }
          }
        },
        {
          "name": "POST /api/profile/resume",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/profile/resume",
            "description": "Replace master resume. Must be a .docx file \u2265 1 KB, \u2264 10 MB. Rate-limited: 10/hr per IP.",
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "resume",
                  "value": "",
                  "type": "file"
                }
              ]
            }
          }
        },
        {
          "name": "POST /api/profile/password",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/profile/password",
            "description": "Change password. Requires current password. Invalidates all existing sessions (password-version fingerprint changes). Sends confirmation email. Rate-limited: 5/hr per IP.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"current_password\": \"old\",\n  \"new_password\": \"new-min-8\"\n}"
            }
          }
        },
        {
          "name": "POST /api/profile/email",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/profile/email",
            "description": "Change email address. Requires current password. Marks account as unverified and sends verification to the new address. Deletes current session cookie \u2014 user must log in again. Rate-limited: 5/hr per IP.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"new_email\": \"new@example.com\",\n  \"current_password\": \"your-password\"\n}"
            }
          }
        }
      ]
    },
    {
      "name": "Calendar",
      "description": "Create and manage calendar events with optional email/Slack reminders. Events can be linked to application tracker records. Reminders fire via the background scheduler (60s polling).\n\n**Event types:** `interview`, `phone_screen`, `deadline`, `follow_up`, `offer`, `other`, `custom`\n\n**Reminder channels:** `email`, `slack`\n\n**Limits:** 1,000 events/user, 10 reminders/event, offset_minutes 0\u2013525,600 (1 year).",
      "item": [
        {
          "name": "GET /api/calendar",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/api/calendar?from=2026-06-01T00:00:00Z&to=2026-06-30T23:59:59Z",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "calendar"
              ],
              "query": [
                {
                  "key": "from",
                  "value": "2026-06-01T00:00:00Z"
                },
                {
                  "key": "to",
                  "value": "2026-06-30T23:59:59Z"
                }
              ]
            },
            "description": "List all events for the current user. Optional ISO datetime range filters: `from` and `to`. Returns `{events: [...]}` sorted by datetime ascending."
          }
        },
        {
          "name": "GET /api/calendar/upcoming",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/calendar/upcoming",
            "description": "Return up to 20 events in the next 7 days. Used by the Slack App Home tab."
          }
        },
        {
          "name": "POST /api/calendar",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/calendar",
            "description": "Create a calendar event. `reminders` is an array of `{offset_minutes, channels}` \u2014 the scheduler fires each reminder at `event_datetime - offset_minutes`. `app_id` must be a valid application ID owned by the user if provided.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"HM Interview \u2014 Salesforce\",\n  \"event_type\": \"interview\",\n  \"datetime\": \"2026-06-15T14:00:00Z\",\n  \"timezone\": \"America/New_York\",\n  \"duration_minutes\": 60,\n  \"notes\": \"Ask about the agentic roadmap.\",\n  \"app_id\": \"optional-application-uuid\",\n  \"run_ids\": [],\n  \"reminders\": [\n    {\"offset_minutes\": 1440, \"channels\": [\"email\", \"slack\"]},\n    {\"offset_minutes\": 60,   \"channels\": [\"slack\"]}\n  ]\n}"
            }
          }
        },
        {
          "name": "GET /api/calendar/{event_id}",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/calendar/{{event_id}}",
            "description": "Get a single event by UUID. Returns 404 if not found or not owned by the current user."
          }
        },
        {
          "name": "PUT /api/calendar/{event_id}",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/calendar/{{event_id}}",
            "description": "Update any event field. All fields optional. If `datetime` changes and `reminders` is not re-specified, existing reminder fire times are automatically recalculated. If `reminders` is provided, old reminders are deleted and replaced.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"datetime\": \"2026-06-16T15:00:00Z\",\n  \"notes\": \"Rescheduled.\"\n}"
            }
          }
        },
        {
          "name": "DELETE /api/calendar/{event_id}",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/api/calendar/{{event_id}}",
            "description": "Delete an event and all its associated reminders. Returns `{\"ok\": true}`."
          }
        }
      ]
    },
    {
      "name": "Application Tracker",
      "description": "Full CRUD for job applications. All data is scoped to the authenticated user. Index fields returned by the list endpoint; full records include `comments`, `audit_log`, and `linked_runs`.\n\n**Valid statuses:** Not Applying, Researching, Applied, Phone Screen, Interviewing, On Hold, Offer, Rejected\n\n**Valid priorities:** Low, Medium, High",
      "item": [
        {
          "name": "GET /api/applications",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/api/applications?page=1&per_page=25&status=Interviewing",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "applications"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "per_page",
                  "value": "25"
                },
                {
                  "key": "status",
                  "value": "Interviewing"
                }
              ]
            },
            "description": "List applications (paginated). Optional filters: `status`, `priority`, `page`, `per_page` (0 = all). Returns `{items, total, page, per_page, pages}`."
          }
        },
        {
          "name": "POST /api/applications",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/applications",
            "description": "Create a new application. `url` must start with http:// or https:// if provided. `company_logo_url` must start with https:// if provided.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"company\": \"Salesforce\",\n  \"domain\": \"salesforce.com\",\n  \"role_title\": \"Solution Engineer\",\n  \"status\": \"Applied\",\n  \"priority\": \"High\",\n  \"url\": \"https://salesforce.com/careers/\u2026\",\n  \"job_source\": \"LinkedIn\",\n  \"location\": \"Remote\",\n  \"salary_range\": \"$130k\u2013$160k\",\n  \"dua\": false\n}"
            }
          }
        },
        {
          "name": "GET /api/applications/{app_id}",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/applications/{{app_id}}",
            "description": "Full application record including `comments`, `audit_log`, and `linked_runs` arrays."
          }
        },
        {
          "name": "PUT /api/applications/{app_id}",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/applications/{{app_id}}",
            "description": "Partial update \u2014 send only changed fields. Diffs are recorded in the application `audit_log`.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"Interviewing\",\n  \"recruiter_name\": \"Jane Smith\",\n  \"recruiter_email\": \"jane@salesforce.com\"\n}"
            }
          }
        },
        {
          "name": "DELETE /api/applications/{app_id}",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/api/applications/{{app_id}}",
            "description": "Delete application. A deletion tombstone is saved before removal for audit purposes. Returns 204."
          }
        },
        {
          "name": "GET /api/applications/{app_id}/audit",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/applications/{{app_id}}/audit",
            "description": "Returns the application-level `audit_log` array (create, update, delete, comment, run-link events)."
          }
        },
        {
          "name": "POST /api/applications/{app_id}/comments",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/applications/{{app_id}}/comments",
            "description": "Add a comment. Returns the created comment object.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"text\": \"Recruiter confirmed HM interview for Monday.\"\n}"
            }
          }
        },
        {
          "name": "PUT /api/applications/{app_id}/comments/{comment_id}",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/applications/{{app_id}}/comments/{{comment_id}}",
            "description": "Edit an existing comment.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"text\": \"Updated note text.\"\n}"
            }
          }
        },
        {
          "name": "DELETE /api/applications/{app_id}/comments/{comment_id}",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/api/applications/{{app_id}}/comments/{{comment_id}}",
            "description": "Delete a comment. Returns 204."
          }
        },
        {
          "name": "POST /api/applications/{app_id}/runs",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/applications/{{app_id}}/runs",
            "description": "Link a Drive run folder to an application. Auto-called when a run is started with `app_id`.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"type\": \"resume\",\n  \"folder_name\": \"Salesforce_SolutionEngineer\",\n  \"folder_url\": \"https://drive.google.com/drive/folders/\u2026\",\n  \"gdrive_folder_id\": \"1abc123\"\n}"
            }
          }
        },
        {
          "name": "DELETE /api/applications/{app_id}/runs/{link_id}",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/api/applications/{{app_id}}/runs/{{link_id}}",
            "description": "Unlink a Drive run from an application. Returns 204."
          }
        }
      ]
    },
    {
      "name": "Companies",
      "item": [
        {
          "name": "GET /api/companies/search",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/api/companies/search?q=salesforce",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "companies",
                "search"
              ],
              "query": [
                {
                  "key": "q",
                  "value": "salesforce"
                }
              ]
            },
            "description": "BrandFetch company search. Public endpoint \u2014 no auth required. Returns up to 5 results: `{name, domain, logo_url, description}`. Used by the tracker and Slack `/track-add` command."
          }
        }
      ]
    },
    {
      "name": "Application Runs",
      "description": "Start and monitor a resume + ATS resume + cover letter generation run.\n\n**Lifecycle:** POST \u2192 SSE stream \u2192 `done` event contains file names \u2192 download files.\n\nRuns are serialised per user (one active at a time per user). Up to 5 in-flight + queued entries per user. Admin accounts cannot create runs.\n\n**`done` event shape:**\n```json\n{\n  \"type\": \"done\",\n  \"run_id\": \"\u2026\",\n  \"framing_angle\": \"\u2026\",\n  \"folder_url\": \"https://drive.google.com/\u2026\",\n  \"replacements_warning\": null,\n  \"files\": {\n    \"resume\": \"Resume_\u2026docx\",\n    \"ats\": \"Resume_\u2026_ATS.docx\",\n    \"cover_letter\": \"CoverLetter_\u2026.docx\"\n  }\n}\n```\n`replacements_warning` is non-null if fewer than 70% of XML edits succeeded \u2014 some resume sections may need manual review.",
      "item": [
        {
          "name": "POST /api/run",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/run",
            "description": "Start a resume generation run. Returns {run_id, machine_id}. Set fly-force-instance-id cookie to machine_id before opening the SSE stream to ensure the stream hits the same Fly.io machine.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"job_posting\": \"Paste full JD here\u2026\",\n  \"company\": \"Acme Corp\",\n  \"role\": \"Solutions Engineer\",\n  \"contact\": \"Jane Smith\",\n  \"app_id\": \"optional-application-uuid\",\n  \"model\": null\n}"
            }
          }
        },
        {
          "name": "GET /api/run/{run_id}/stream",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/run/{{run_id}}/stream",
            "description": "SSE stream. Events: `progress` (log line), `done` (files + folder_url + replacements_warning), `error`. Keep-alives every 30s.",
            "header": [
              {
                "key": "Accept",
                "value": "text/event-stream"
              }
            ]
          }
        },
        {
          "name": "GET /api/run/{run_id}/status",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/run/{{run_id}}/status",
            "description": "Poll run status: `queued | running | done | error`. Returns `{run_id, status, error}`."
          }
        },
        {
          "name": "GET /api/run/{run_id}/files/{filename}",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/run/{{run_id}}/files/Resume_CoreyLaverdiere_Acme_SolutionsEngineer.docx",
            "description": "Download output DOCX. Filename from `files` in the `done` SSE event. Returns 403 if run belongs to another user (admins can download any run's files). Path-traversal protected via resolve/relative_to check."
          }
        }
      ]
    },
    {
      "name": "Interview Prep Runs",
      "description": "Same lifecycle as Application Runs. `done` event returns `files.prep` (single DOCX). Output is a two-column 9-section reference card: Know Your Interviewer, Role Fit Map, Gap Bridge, Development Framework, Anchor Stories, Likely Questions, Questions to Ask, Differentiating Edge, Closing Line.\n\n**Valid `round_type` values:** `Phone Screen`, `Hiring Manager`, `Peer`, `Technical`, `Executive`, `Panel`",
      "item": [
        {
          "name": "POST /api/prep",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/prep",
            "description": "Start an interview prep run. Returns {prep_id, machine_id}. Set fly-force-instance-id cookie to machine_id before opening the SSE stream.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"job_posting\": \"Paste full JD here\u2026\",\n  \"company\": \"Acme Corp\",\n  \"role\": \"Solutions Engineer\",\n  \"round_type\": \"Hiring Manager\",\n  \"interviewer\": \"Jane Smith \u2014 Engineering Manager\",\n  \"focus\": \"Lean into the agentic AI angle\",\n  \"app_id\": \"optional-application-uuid\",\n  \"model\": null\n}"
            }
          }
        },
        {
          "name": "GET /api/prep/{prep_id}/stream",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/prep/{{prep_id}}/stream",
            "description": "SSE stream for prep run. Same event format as application runs.",
            "header": [
              {
                "key": "Accept",
                "value": "text/event-stream"
              }
            ]
          }
        },
        {
          "name": "GET /api/prep/{prep_id}/status",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/prep/{{prep_id}}/status",
            "description": "Poll prep status: `queued | running | done | error`."
          }
        },
        {
          "name": "GET /api/prep/{prep_id}/files/{filename}",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/prep/{{prep_id}}/files/InterviewPrep_CoreyLaverdiere_Acme_SolutionsEngineer_HiringManager.docx",
            "description": "Download prep DOCX. Returns 403 if prep belongs to another user."
          }
        }
      ]
    },
    {
      "name": "Google Drive",
      "description": "Proxy endpoints for listing run folders and fetching saved job descriptions. Drive is configured server-side via GDRIVE_TOKEN_JSON secret.\n\nFolder ownership is verified before fetching job postings \u2014 the API fails closed (503) if listing fails, rather than allowing arbitrary folder reads.",
      "item": [
        {
          "name": "GET /api/gdrive/runs",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/gdrive/runs",
            "description": "List all run folders for the current user from Drive. Checks both the user's personal subfolder and the legacy flat root. Each entry: `{id, name, web_view_link, source: 'user'|'legacy'}`."
          }
        },
        {
          "name": "GET /api/gdrive/runs/{folder_id}/job_posting",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/gdrive/runs/{{folder_id}}/job_posting",
            "description": "Fetch `job_posting.txt` from a Drive folder. Returns 404 if not saved. Returns 403 if the folder is not in the user's run folder list. Returns 503 if Drive listing fails (fail-closed ownership check)."
          }
        },
        {
          "name": "Save Job Description to Drive",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/api/gdrive/runs/{{folder_id}}/job_posting",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "gdrive",
                "runs",
                "{{folder_id}}",
                "job_posting"
              ]
            },
            "description": "Upsert job_description.md in a Drive folder. Deletes any existing copy first. Ownership verified.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"job_description\": \"# Job Title\\n\\nFull job description in markdown...\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Local Runs",
      "item": [
        {
          "name": "List Local Run Folders",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/api/runs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "runs"
              ]
            },
            "description": "List all local run folders for the authenticated user, sorted by modification time. Returns {runs: [{folder, has_job_posting}]}."
          },
          "response": []
        },
        {
          "name": "Get Local Run Job Posting",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/api/runs/{{folder_name}}/job_posting",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "runs",
                "{{folder_name}}",
                "job_posting"
              ]
            },
            "description": "Fetch the saved job posting text from a local run folder (job_posting.txt). Used by the prep form dropdown."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Model Config",
      "description": "Get and set the active Claude model. GET is available to all authenticated users; PUT and list are admin-only.",
      "item": [
        {
          "name": "GET /api/config/model",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/config/model",
            "description": "Returns `{model, default}` \u2014 the currently active model and the compile-time default."
          }
        },
        {
          "name": "PUT /api/config/model",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/config/model",
            "description": "Set the active model (admin only). Persisted to Tigris; takes effect immediately for all new runs.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"model\": \"claude-sonnet-4-6\"\n}"
            }
          }
        },
        {
          "name": "GET /api/config/models",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/config/models",
            "description": "List all allowed model IDs and the currently active one (admin only)."
          }
        }
      ]
    },
    {
      "name": "Audit",
      "item": [
        {
          "name": "GET /api/audit/me",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/audit/me",
            "description": "Returns the current user's audit event log, newest first (up to 500 events). Each event: `{id, action, actor, timestamp, ip, details}`. Events are stored as individual S3 objects \u2014 no cross-machine race conditions."
          }
        }
      ]
    },
    {
      "name": "Admin \u2014 Users",
      "description": "All endpoints require `role: admin`. Accept the session cookie or `Authorization: Bearer {BOT_API_KEY}`.\n\nRole and active-status changes are immediately reflected because the per-request user cache is invalidated on write.",
      "item": [
        {
          "name": "GET /api/admin/users",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/users",
            "description": "List all users with `app_count`, `last_login`, `email_verified`, `active`, `role` status."
          }
        },
        {
          "name": "PUT /api/admin/users/{user_id}",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/admin/users/{{user_id}}",
            "description": "Edit `display_name`, `email` (safe rename across all S3 indexes), `role`, `email_verified`, `active`. Changes are reflected immediately \u2014 user cache is invalidated after write.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"role\": \"admin\",\n  \"active\": true,\n  \"email_verified\": true\n}"
            }
          }
        },
        {
          "name": "POST /api/admin/users/{user_id}/resend-verification",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/admin/users/{{user_id}}/resend-verification",
            "description": "Resend verification email to a specific user as admin."
          }
        }
      ]
    },
    {
      "name": "Admin \u2014 Applications",
      "item": [
        {
          "name": "GET /api/admin/applications",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/applications",
            "description": "All applications across all users. Each item includes `_user_id` and `_user_email`."
          }
        },
        {
          "name": "GET /api/admin/applications/{user_id}/{app_id}",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/applications/{{user_id}}/{{app_id}}",
            "description": "Full application record for any user."
          }
        },
        {
          "name": "PUT /api/admin/applications/{user_id}/{app_id}",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/admin/applications/{{user_id}}/{{app_id}}",
            "description": "Admin update any application.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"Rejected\"\n}"
            }
          }
        },
        {
          "name": "DELETE /api/admin/applications/{user_id}/{app_id}",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/api/admin/applications/{{user_id}}/{{app_id}}",
            "description": "Admin delete any application. Returns 204."
          }
        },
        {
          "name": "POST /api/admin/applications/{user_id}/{app_id}/comments",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/admin/applications/{{user_id}}/{{app_id}}/comments",
            "description": "Admin add comment to any application.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"text\": \"Admin note.\"\n}"
            }
          }
        }
      ]
    },
    {
      "name": "Admin \u2014 Runs",
      "item": [
        {
          "name": "GET /api/admin/runs",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/runs",
            "description": "All Drive run folders across all users with type detection (resume vs interview_prep), `created_at`, and `web_view_link`. Also includes any still-active in-memory runs."
          }
        }
      ]
    },
    {
      "name": "Admin \u2014 Audit Log",
      "item": [
        {
          "name": "GET /api/admin/audit",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{base_url}}/api/admin/audit?page=1&per_page=50&sort_order=desc",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "admin",
                "audit"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "per_page",
                  "value": "50"
                },
                {
                  "key": "sort_order",
                  "value": "desc"
                },
                {
                  "key": "action",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "actor",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "user_id",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "source",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "event_id",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "from_ts",
                  "value": "",
                  "disabled": true
                },
                {
                  "key": "to_ts",
                  "value": "",
                  "disabled": true
                }
              ]
            },
            "description": "Unified paginated audit log across all users and all application records. Each event is an individual S3 object (no cross-machine write races). Filters: `action`, `actor`, `user_id`, `source` (user|application), `event_id`, `from_ts`, `to_ts` (ISO), `sort_order` (asc|desc)."
          }
        },
        {
          "name": "GET /api/admin/audit/export",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/audit/export",
            "description": "Full filtered audit result with no pagination. Same filters as GET /api/admin/audit. Logs `admin_csv_export` event. Used for CSV/Excel exports from the admin dashboard."
          }
        },
        {
          "name": "GET /api/admin/audit/action-types",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/audit/action-types",
            "description": "Returns sorted list of all known audit action type strings for filter dropdowns."
          }
        },
        {
          "name": "POST /api/admin/log-activity",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/admin/log-activity",
            "description": "Log a custom admin activity event (e.g. CSV export triggered from the browser). Writes `admin_csv_export` to the audit log.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"screen\": \"users_csv\",\n  \"row_count\": 2,\n  \"filters\": {\"role\": \"admin\"}\n}"
            }
          }
        }
      ]
    },
    {
      "name": "Admin \u2014 Webhooks",
      "description": "Create and manage outbound webhooks for event streaming. Webhooks fire on every `user_audit.log()` call that matches subscribed events and active filters.\n\n**Payload formats:** `generic` | `slack` | `ms_teams` | `grafana_loki`\n\n**Filters (all AND, blank = skip):**\n- `filter_actors`: comma-separated emails/user_ids\n- `filter_source`: `user` | `application`\n- `filter_categories`: array of `auth|profile|applications|runs|admin`\n- `filter_app_id`: specific application UUID\n\n**Security:** `secret` is stored encrypted at rest with AES-256-GCM. If set, each delivery includes `X-Hub-Signature-256: sha256={hmac}`. Rotating `SESSION_SECRET` invalidates stored secrets \u2014 re-save each webhook to re-encrypt. SSRF guard is applied at both creation time and delivery time.",
      "item": [
        {
          "name": "GET /api/admin/webhooks",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/webhooks",
            "description": "List all webhooks (summary from index \u2014 secrets not included)."
          }
        },
        {
          "name": "POST /api/admin/webhooks",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/admin/webhooks",
            "description": "Create a webhook. `url` is validated against SSRF (DNS resolution + private-net check). `secret` is encrypted with AES-256-GCM before storage.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Slack \u2014 Auth Events\",\n  \"url\": \"https://hooks.slack.com/services/\u2026\",\n  \"payload_format\": \"slack\",\n  \"events\": [\"*\"],\n  \"secret\": \"\",\n  \"active\": true,\n  \"filter_actors\": \"cdl825@gmail.com\",\n  \"filter_source\": \"user\",\n  \"filter_categories\": [\"auth\"],\n  \"filter_app_id\": \"\",\n  \"headers\": {},\n  \"query_params\": {}\n}"
            }
          }
        },
        {
          "name": "GET /api/admin/webhooks/{webhook_id}",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/webhooks/{{webhook_id}}",
            "description": "Full webhook record including `recent_deliveries` (last 25) and `delivery_stats`. Secret is redacted in the response (first/last 4 chars shown)."
          }
        },
        {
          "name": "PUT /api/admin/webhooks/{webhook_id}",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/api/admin/webhooks/{{webhook_id}}",
            "description": "Update any webhook field. All fields optional. Re-saving with a `secret` re-encrypts it under the current SESSION_SECRET.",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"active\": false\n}"
            }
          }
        },
        {
          "name": "DELETE /api/admin/webhooks/{webhook_id}",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/api/admin/webhooks/{{webhook_id}}",
            "description": "Delete webhook. Returns 204."
          }
        },
        {
          "name": "POST /api/admin/webhooks/{webhook_id}/test",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/api/admin/webhooks/{{webhook_id}}/test",
            "description": "Send a synchronous test delivery with `action: webhook_test`. Returns delivery result: `{id, timestamp, event_action, status_code, success, error, duration_ms}`. SSRF guard is applied before the request is sent."
          }
        },
        {
          "name": "GET /api/admin/webhooks/{webhook_id}/deliveries",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/api/admin/webhooks/{{webhook_id}}/deliveries",
            "description": "Last 25 deliveries for a webhook, sorted descending by timestamp. Each entry includes `status_code`, `success`, `error`, `duration_ms`. Blocked deliveries (SSRF) appear with `error: Delivery blocked: URL resolved to a private/internal address`."
          }
        }
      ]
    }
  ]
}